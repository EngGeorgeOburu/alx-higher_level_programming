#!/usr/bin/python3
"""
Text-indentation function.
"""


def text_indentation(text):
    """
    Find max integer in a list, if none return none
    Args:
        text (list): A liat containing integers.
    Return:
        int: max integer in the list
        None: If the list is empty.
    """
    if len(list) == 0:
        return None
    result = list[0]
    j = 1
    while j < len(list):
        if list[i] > result:
            result = list[i]
        i += 1
        return result
